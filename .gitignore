# OS
.DS_Store
Thumbs.db

# Environment and secrets
.env
.env.*
!.env.example

# Local DB copies (real DB lives in OS app data; ignore if ever dropped in-repo)
*.db
*.sqlite
*.sqlite3

# Keys, certs, keystores
*.pem
*.p12
*.pfx
*.jks
*.keystore
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub

# npm auth tokens sometimes live here; remove this line if you commit a token-free .npmrc
.npmrc

# Cursor MCP config often contains API keys
.cursor/mcp.json
